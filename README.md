# TPLECOMMERCE Backend

This is the backend for the TPLECOMMERCE project, built with FastAPI.

## Development

```bash
pip install -r requirements.txt
uvicorn main:app --reload
```

## Deployment

The backend is containerized using Docker and can be deployed on Render.
